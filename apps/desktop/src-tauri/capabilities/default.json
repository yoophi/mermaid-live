{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Mermaid Live application window.",
  "windows": ["main", "about-window", "editor-*", "editor-tab-*", "clipboard-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
